# Implement (Subagent Workflow)

Запусти процесс разработки **Subagent Workflow** из skill `subagent-workflow`: Worker → Code reviewer → Worker (при необходимости).

## Что делать

1. **Worker (реализация)**  
   Запусти сабагента **worker** (`subagent_type: "worker"`) с чётким описанием задачи.  
   Используй как задачу текст, который пользователь написал после `/implement`, или уточни задачу у пользователя.  
   Дождись завершения worker.

2. **Code reviewer (ревью)**  
   После завершения worker запусти сабагента **code-reviewer** (`subagent_type: "code-reviewer"`).  
   Передай контекст: что было сделано, какие файлы/области кода затронуты. Ревьюер посмотрит изменения (например, через git diff).

3. **Результаты ревью**  
   Ревьюер должен явно описать:
   - критические ошибки (баги, неверное поведение);
   - недостатки (безопасность, поддерживаемость, корректность);
   - возможности оптимизации (производительность, ясность, структура).  
   Если критичных замечаний и важных оптимизаций нет — на этом шаге процесс можно завершить.

4. **Worker (исправления по ревью)**  
   Если у ревьюера есть **критические ошибки**, **недостатки** или **оптимизации**, которые нужно внедрить:
   - снова запусти **worker** с задачей: внедрить **все** предложения ревьюера (перечисли или кратко опиши их в задаче).  
   После выполнения при необходимости можно ещё раз запустить code-reviewer для проверки; иначе считай процесс завершённым.

## Порядок

- Сначала всегда **worker**, потом **reviewer**. Для новой задачи reviewer не запускается до worker.
- Второй запуск worker — только если у ревьюера есть конкретные, применимые замечания.

Формулируй задачи для сабагентов ясно и самодостаточно, чтобы они могли выполнить их без перечитывания всего чата.
