/**
 * Base URLs and API endpoints.
 * Playwright baseURL is set in playwright.config.ts; use this for reference or future API calls.
 */
export const APP_URL = 'https://partner.stage2.surfsight.net';
